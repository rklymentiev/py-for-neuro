my_string = "P@#yn26at^&i5ve"

# set the initial values for the counter
word_characters_count = ___
digits_count = ___
symbols_count = ___

# iterate over the string
for ___ in ____:
    if ____:   # check if a word character ('a', 'b', 'c')
        word_characters_count = ____
    elif ____: # check if a digit (1, 2, 3)
         digits_count = ___
    else:      # the rest are symbols ('%', '@', '+')
        symbols_count = ___

# add resulting values to a dictionary
counts = {
    "Word Characters": ___,
    "Digits": ___,
    "Symbols": ___
    }
print(counts)
